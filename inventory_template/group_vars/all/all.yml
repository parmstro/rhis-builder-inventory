---
# Async control variables are defined as role defaults in each consuming project
# using a role-prefixed name: satellite_async_timeout/satellite_async_delay
# (rhis-builder-satellite), idm_async_timeout/idm_async_delay (rhis-builder-idm,
# pending migration), kvm_async_timeout/kvm_async_delay (rhis-builder-kvm, pending).
# Per-host overrides live in the respective host_vars satellite_pre.yml files.
# Image Builder variables are defined in group_vars/imagebuilders/imagebuilder_build.yml.
# See schema/variable_migration.md for migration status of unqualified async_ names.
...
