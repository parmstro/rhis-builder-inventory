---
# transfer_to_drive_vars.yml
# Generated by export_deployment.yml at {{ log_timestamp }}
# DO NOT EDIT — regenerate by re-running export_deployment.sh on the lowside.
#
# Run-specific values for transfer_to_drive.yml.
# Pass to ansible-playbook with: --extra-vars @transfer_to_drive_vars.yml

transfer_provisioner_host: "{{ provisioner_host }}"
transfer_sat_host: "{{ sat_host }}"
transfer_export_staging: "{{ export_staging }}"
transfer_pulp_export_path: "{{ pulp_export_path | default('') }}"
transfer_pulp_src_root: "{{ pulp_src_root | default('') }}"
transfer_pulp_org_dir: "{{ pulp_org_dir | default('') }}"
transfer_highside_domain: "{{ highside_domain }}"
transfer_log_timestamp: "{{ log_timestamp }}"
transfer_rhel_dvd_iso_path: "{{ basevars_rhel_dvd_iso_path | default('') }}"
transfer_sat_dvd_iso_path: "{{ basevars_satellite_dvd_iso_path | default('') }}"
